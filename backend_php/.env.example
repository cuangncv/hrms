# Sao chép file này thành ".env" rồi chỉnh giá trị cho phù hợp môi trường thực tế.
# KHÔNG commit file .env thật (đã có trong .gitignore).

JWT_SECRET=doi-chuoi-nay-thanh-mot-chuoi-ngau-nhien-that-dai-va-bi-mat
JWT_EXPIRES=604800

DB_HOST=localhost
DB_NAME=hrms
DB_USER=root
DB_PASS=
